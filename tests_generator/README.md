# Description

This crate shall be compiled for the HOST target.
It generate rust integration test in folder  `generated_tests` of parent folder.

This tools works only when invoked from cargo because it use cargo enviromental variable.